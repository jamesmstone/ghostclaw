repos:
  - repo: local
    hooks:
      - id: ghostclaw-rust-checks
        name: ghostclaw rust checks
        entry: bash -c 'cargo fmt --check && cargo clippy -- -D warnings && cargo test'
        language: system
        pass_filenames: false
